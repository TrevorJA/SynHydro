[project]
name = "synhydro"
dynamic = ["version"]
description = "Synthetic Generation Library - Methods and models for generating synthetic hydrologic timeseries."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    {name = "Trevor Amestoy", email = "tja73@cornell.edu"},
]
keywords = ["hydrology", "synthetic generation", "streamflow", "stochastic", "time series"]
classifiers = [
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Hydrology",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "numpy>=2.2",
    "pandas>=2.3",
    "matplotlib>=3.10",
    "scipy>=1.15",
    "scikit-learn>=1.5",
    "hmmlearn>=0.3.3",
    "pywavelets>=1.8",
    "seaborn>=0.13",
    "spei>=0.8.2",
    "h5py>=3.16",
]

[project.optional-dependencies]
dev = [
    "ipython",
    "ipykernel",
    "nbclient",
    "nbformat",
    "pygeohydro",
    "pytest",
    "pytest-cov",
]
test = [
    "pytest",
    "pytest-cov",
]
lint = [
    "pre-commit>=2.20.0",
]
docs = [
    "mkdocs-material>=9.5,<9.7",
    "mkdocstrings[python]>=0.24",
    "mkdocs-jupyter>=0.24",
]

[project.urls]
Homepage = "https://github.com/TrevorJA/SynHydro"
Repository = "https://github.com/TrevorJA/SynHydro"
Documentation = "https://trevorja.github.io/SynHydro/"
Issues = "https://github.com/TrevorJA/SynHydro/issues"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/synhydro/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/synhydro"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/tests",
    "/LICENSE",
    "/README.md",
    "/CHANGELOG.md",
    "/CITATION.cff",
    "/pyproject.toml",
]

[tool.black]
line-length = 88
target-version = ["py310", "py311", "py312"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
norecursedirs = ["_dev"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "-ra",
]
filterwarnings = [
    # hmmlearn.utils.normalize uses in-place ``arr.shape = ...`` assignment,
    # deprecated in NumPy 2.5. Third-party; nothing to fix in SynHydro.
    "ignore:Setting the shape on a NumPy array:DeprecationWarning",
]
